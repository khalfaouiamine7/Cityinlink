{% extends 'back.html.twig' %}

{% block title %}New Specialite{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="mt-4 mb-3">Create new Specialite</h1>
            {{ include('specialite/_form.html.twig') }}
            <a href="{{ path('app_specialite_index') }}" class="btn btn-secondary mt-3">Back to List</a>
        </div>
    </div>
</div>
{% endblock %}
