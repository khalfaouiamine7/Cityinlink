<form method="post" action="{{ path('app_consultaion_delete', {'id': consultaion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ consultaion.id) }}">
    <button class="btn">Delete</button>
</form>
