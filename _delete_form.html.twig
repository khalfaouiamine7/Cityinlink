<form method="post" action="{{ path('app_medicament_delete', {'idMedicament': medicament.idMedicament}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medicament.idMedicament) }}">
    <button class="btn">Delete</button>
</form>
